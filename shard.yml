name: switch-proxy
version: 0.1.0

authors:
  - your-name-here <your-email-here>

targets:
  switch-proxy:
    main: src/switch-proxy.cr

crystal: 0.32.0

dependencies:
  clim:
    github: at-grandpa/clim

license: MIT
